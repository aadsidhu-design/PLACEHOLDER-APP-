
import SwiftUI

struct MilestonesView: View {
    var body: some View {
        VStack {
            Text("Milestones")
                .font(.largeTitle)
                .padding()
            Text("Your milestones will appear here.")
            // List of milestones will go here
        }
    }
}
